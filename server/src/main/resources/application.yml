spring:
  servlet:
    multipart:
      max-file-size: 15MB
      max-request-size: 15MB
  
  datasource:
    url: jdbc:mysql://localhost:3306/expiry_date
    username: root
    password: Kai99132003@
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true